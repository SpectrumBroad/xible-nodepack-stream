{
	"name": "stream.readables.onend",
	"type": "event",
	"description": "Triggered once all of the stream(s) have no more data available.",
	"inputs": {
		"streams": {
			"type": "stream.readable",
			"description": "The stream(s) to monitor."
		}
	},
	"outputs": {
		"trigger": {
			"type": "trigger"
		}
	}
}
